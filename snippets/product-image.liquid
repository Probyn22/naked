{% assign product_image_url = product.featured_image.src | img_url: 'large' %}
<img src="{{ product_image_url }}" alt="{{ product.title }}" />
{% for image in product.images %}
  <img src="{{ image.src | img_url: 'large' }}" alt="{{ product.title }}" />
{% endfor %}
